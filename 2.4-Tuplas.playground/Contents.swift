import Cocoa

/* -- TUPLAS -- */

// Solo valores del mismo tipo
var color = ("ff0000", "Rojo")

// Valores de distintos tipos
var alumno = ("Diego López", 33)

// Clave y Valor de distintos tipos
var empleado = (nombre: "Facundo Collado", edad: 44, casado: false)

/* Ejemplo de uso común con color RGB */
var (r, g, b) = (150, 150, 150)

print(r)
print(g) 
print(b)
